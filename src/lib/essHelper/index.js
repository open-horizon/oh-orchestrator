const {
  hzn: {
    essObjectTypes,
  },
} = require('../../configuration/config');

const setupESSObjectWebhook = (nodeId, correlationId) => {
  

  const 

};

module.exports = {
  initializeObjectPolling,
};
